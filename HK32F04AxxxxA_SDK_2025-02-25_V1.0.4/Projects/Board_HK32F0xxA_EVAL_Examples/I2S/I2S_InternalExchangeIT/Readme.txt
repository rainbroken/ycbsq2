/*
* @file name: Readme
* @author: AE Team
*/

V1.0.0/2023-10-08
1. 首次发布

******************************例程详细描述*****************************
功能描述:
1. 此示例提供了一个小型应用程序，其中按钮用于使用中断并通过使用I2S固件库来触发I2S通信；
2. 此应用使用HK32应用板的两个I2S进行通信；
3. 这两个设备都是飞利浦标准配置，具有16位的数据包和16KHz的音频。
4. I2S发送中断和I2S接收中断都被启用。在这些中断子程序中，I2S_Buffer_Tx被传输，
并且接收到的值被加载到I2S_Buffer_Rx缓冲器中；
5. 当用户按下主板上的SEL按钮时，主发送器开始发送存储在I2S_Buffer_Tx缓冲区中的数据，
而从板接收数据I2S_Buffer_Rx缓冲区。
6. 传输完成后，会进行比较，TransferStatus会给出数据传输状态：
- 如果传输和接收的数据相同，则TransferStatus PASSED：绿色LED亮起。
- 如果传输和接收的数据不同，则传输状态失败：红色LED亮起。

时钟配置:
1. 本例程采用HSI56MHz作为系统时钟，用户如果需要更改系统时钟配置，
需要配置修改SYSCLK_SOURCE宏定义的。
2. 关于系统时钟如何配置，请参考对应用户手册或者RCC库函数及RCC相关例程。

适用芯片:
HK32F04AxxA

涉及外设:
I2S1
I2S2
GPIO

涉及管脚:
I2S1_WS - PA4
I2S1_CK - PA5
I2S1_SD - PA7
I2S2_WS - PB12
I2S2_CK - PB13
I2S2_SD - PB15
PC10 - LED1
PC12 - LED3
PA0 - BUTTON_SEL

注意事项:
1. 推荐使用MDK5.00及其以上版本编译本工程（MDK - ARM）
或者IAR8.22及其以上版本编译本工程(EWARM);
2. 使用之前检测电源VCC、GND等是否正常。

如何使用:
1. 直接编译程序下载到评估版，重启评估版;

***********************************************************************
